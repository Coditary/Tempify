# Tempify Registry

Small starter registry for Tempify templates.

Current catalog file:

- `templates.json`

Local development expects `source.url` entries to point at sibling `../tempify-templates` repo.
